language: python
python:
  - "3.4"
  - "3.5"
  - "3.6"
# command to install dependencies
install:
  - pip install tox-travis coveralls
# command to run tests
script:
  - tox
after_success:
  - coveralls
